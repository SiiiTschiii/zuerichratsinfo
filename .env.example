# X API Credentials (OAuth 1.0a User Context)
# Get these from https://developer.x.com/en/portal/dashboard
# Under "Projects & Apps" -> Your App -> "Keys and tokens"

# Consumer Keys (API Key and Secret)
X_API_KEY=
X_API_SECRET=

# Authentication Tokens (Access Token and Secret)
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# Rate Limiting Configuration
# How many recent votes to check for new ones
MAX_VOTES_TO_CHECK=4

# Maximum posts per run to avoid API rate limits
# X Free tier: 17 posts per 24h, 500 per month
X_MAX_POSTS_PER_RUN=2
