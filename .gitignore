# Environment variables
.env
.env.test

# Binary
zurichratsinfo

# macOS
.DS_Store

data/email_overrides.yaml

# Outreach campaign recipient lists contain personal emails — keep local.
# Only the schema example is committed.
data/campaign_recipients/*
!data/campaign_recipients/recipients.example.yaml

out/

# Claude Code (local, machine-specific)
.claude/settings.local.json
.claude/worktrees/

# Vote state log lives on the `state-log` branch, not here. The bot restores
# these into the checkout at runtime; they must never be committed to main.
data/posted_votes_*.json
